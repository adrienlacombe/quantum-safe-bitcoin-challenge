# Pinning research log

## Starting state (2026-09-11)

- Checkout: `356afa3feb0645eb0f0835cb15696b5dfaf69597`, cloned with
  `yukon clone eigenlabs/starkware-challenge/pinning`.
- Read the installed Yukon skill before cloning and again from the printed
  benchmark work directory. Read the schema-v2 manifest, problem/scoring specs,
  setup script, local GPU wrapper, and all three candidate file interfaces.
- Only `candidates/pinning/` is editable. Harness settings and verification
  remain authoritative. Build artifacts generated by setup are not source edits.
- Local `yukon setup --track pinning` passed the CPU verifier smoke test.
  This workstation has neither CUDA nor a GPU.
- A Vast RTX 4090 with 450 W limit and CUDA 12.6.85 has been provisioned.
  GPU setup and measurements are pending repository transfer authorization.
  No measured baseline and no optimization claim yet.

## Resolved blockers

1. Sandbox networking: use reviewed network-enabled tool calls.
2. Missing Git and unavailable sudo: extracted Debian Git packages into the
   user-local installation. Set `GIT_EXEC_PATH` to its installed helper directory.
3. Skill installer initially reported success without creating a target:
   created the agents skill directory, reran installation, and read the actual file.
4. GitHub authentication: user registered the existing public SSH key; verified
   authentication and configured GitHub HTTPS URLs to use SSH. Clone succeeded.

## Existing frontier and lessons

Yukon reports promoted submission `99521b7`, commit `e5576bd`, at
235,880,780 verified candidates/s on its RTX 4090. Its public note reports a
separate 300-second local score of 249,731,643 with 8,950 verified hits.
These are different environments and must not be treated as our baseline.

That promoted change removed debug payloads from the production kernel. The
note reports zero hot-kernel spills after removal. It also reports rejected
128-thread launch geometry, a problem-specific lookup table, SHA-midstate,
and warp-batch-inversion experiments. These are prior claims to investigate,
not locally reproduced conclusions. Research Discussions are disabled.

## Experiment process

1. Build and run the unchanged candidate through Yukon and the documented
   `cmd:python3 harness/gpu_wrap.py --src candidates/pinning/pinning.cu` adapter.
   Use N=24 and the default 1,200-second window for the baseline. Record the
   seed, GPU/toolchain, elapsed time, verified hits, score, and source revision.
2. Make one attributable hypothesis per experiment. Preserve the starting
   source before changes. Use short verified runs to reject poor approaches
   quickly, then longer samples for improvements close to statistical noise.
3. Compare on the same GPU with the same toolchain. Every reported hit must
   independently verify; self-reported M/s alone is not evidence for promotion.
4. Record failures and revert only our own experimental changes. Revisit a
   failed idea only with a concrete change that addresses its measured cost.
5. Publish detailed submission notes after validated improvements, with exact
   commands, results, limitations, attribution, and rejected experiments.

## Hypotheses awaiting baseline

- The hot kernel assembles a 192-byte suffix buffer despite a 75-byte suffix.
  Host-side padded word templates could reduce local memory traffic; offsets
  and cross-word patches must remain correct for generated problem instances.
- Fixed-base multiplication reads a 64 MiB point table using 16-bit windows.
  Smaller windows trade additional point additions for improved cache locality.
- One modular inverse per thread remains expensive. Any cross-thread batching
  must justify shuffle, register, divergence, and partial-batch costs against
  the prior failed experiment.

No hypothesis has been implemented or validated in this log's initial state.

## GPU baseline and first hypothesis

The user authorized transfer to the provisioned Vast instance. GPU setup passed
with CUDA 12.6.85, driver 580.159.03, RTX 4090, and a 450 W limit. The unmodified
candidate baseline is running with seed `565864696`, N=24, and 1,200 seconds:

```sh
QSB_VERIFY_WORKERS=8 \
QSB_GRINDER="cmd:python3 harness/gpu_wrap.py --src candidates/pinning/pinning.cu" \
yukon run --track pinning
```

Only the documented local GPU adapter replaces the runner-private bridge.
The verifier worker count controls parallelism, not the verification rules.
The first run generated the deterministic 64 MiB G-table inside the measured
process. Record this cold-start cost when comparing against later warm runs.

The local working copy now contains an **untested GPU prototype**, while the
remote baseline source and executable remain unchanged. The prototype computes
the first 64-byte tail block on the host once per sequence, then hashes the
remaining 11 bytes using statically packed GPU words. This removes one SHA
compression per candidate and the dynamically indexed suffix buffer from the
specialized path. It also specializes the normal single-hash mode; alternate
layouts and modes retain the generic path. No EC arithmetic changes.

`python3 candidates/pinning/check_tail_words.py` passed 2,320 SHA256d comparisons
against hashlib across 16 synthetic prefixes, including sequence and locktime
boundaries. This establishes the intended host-state/word-packing algebra,
not CUDA correctness or performance. GPU build, independent hit verification,
and matched performance comparison are still required.

NVIDIA's [private-array indexing article](https://developer.nvidia.com/blog/fast-dynamic-indexing-private-arrays-cuda/)
explains why static array indexing can permit register allocation while runtime
indices require local memory. That motivates specialization; the benefit in
this kernel must be measured. Its old architecture-specific speedups do not
predict the RTX 4090 result.

Read prior submission `8582886`: it reports small local gains from shuffle
inversion and larger batches, but the ranked result missed promotion. Its
block-wide inversion also initially failed verification. This strengthens the
case for checking mathematical correctness independently and using matched
longer runs before claiming small improvements.

Early baseline check: copied a live snapshot containing 8,655 complete hits
and independently re-derived 48 samples (16 each from beginning, middle, and
end) using the unchanged `harness/problem.py` and `harness/crypto.py`. All
48 passed N=24. This is only an early failure check; the full run still needs
all-hit verification and a final score. Baseline binary resource inspection
reported 127 registers and a 512-byte stack frame (embedded sm_52 code;
the driver JITs PTX for the actual RTX 4090).

## Frontier audit and literature

The current source was checked byte-for-byte against promoted commit `e5576bd`.
The official progression is 145,499,057 (seed), 197,315,101 (launch bounds and
1M batches), 231,684,949 (projective `u1G` with shared inversion), then
235,880,780 (diagnostic removal). The next 1% bar is about 238,239,588.

Officially rejected variants include 128-thread/four-block launch geometry,
two forms of warp batch inversion, and shuffle inversion plus an 8M batch.
The 8M batch has not been isolated on the current lean kernel. Public proxy
scores moved differently from official scores, so short-run verified scores
are filters rather than promotion evidence.

Submission `1e4b4c7` reports an earlier first-block SHA hoist at about 237.1M/s.
All emitted hits verified, but hit-implied work was materially below its
candidate counter; exact counts were not published. Our decisive checks must
therefore include full verification and candidate-count consistency, not only
valid sample hits or self-reported rate.

The [NVIDIA Ada tuning guide](https://docs.nvidia.com/cuda/ada-tuning-guide/)
documents a 64K-register file per SM, 48 maximum warps per SM, and the large
AD102 L2 cache. The [CUDA best-practices guide](https://docs.nvidia.com/cuda/cuda-c-best-practices-guide/)
warns that higher occupancy does not automatically improve performance and
recommends measuring launch bounds, registers, and spills together. This agrees
with the official regression from forcing four blocks per SM.

The [gECC paper](https://arxiv.org/abs/2501.03245) identifies modular inversion,
integer multiply-add count, and big-integer memory layout as GPU ECC bottlenecks.
Its batching result is relevant background, but public attempts show that a
correct mathematical reduction can still lose here through shuffles, live
state, and spills. Any revisit is gated on profiler evidence.

A stronger untouched algebraic hypothesis is to form `P+C` and `P-C` together
from projective `P=u1G`. Both additions share their `V=xC*Z-X` terms and end
with the same projective Z. Relative to the current sequential `P+C`, then
`(P+C)-2C`, this can save nine field multiplies and one square per candidate,
including cheaper common-Z normalization. A separate 5,000-vector CPU property
test reproduced both affine results. This remains a later prototype; the SHA
experiment stays isolated first.

## Completed untouched baseline

The exact `yukon run --track pinning` baseline finished successfully on the
Vast RTX 4090. The remote source SHA-256 was
`0fe9c262946c02008ef224b2905a51e2e6152e7fa24d6cc1464ac94b0f1ec19c`,
which matches `HEAD:candidates/pinning/pinning.cu`. Results:

- Problem seed: `565864696`; N=24; requested GPU time: 1,200 seconds.
- Candidate-reported peak rate: 239.6 M/s over 1,200.089 seconds.
- Candidate-count estimate: 287,541,246,878. This is diagnostic because the
  wrapper extrapolates the peak printed rate when `timeout` ends the process.
- Verified hits: 33,776 / 33,776, all unique.
- Harness elapsed: 1,200.7 seconds.
- Hit-implied work: 283,333,623,808 candidates.
- Ranked score: **235,973,705 verified candidates/s** (PASS).
- Hit relative standard deviation: 0.5441%.

The first run built the 64 MiB fixed-base table inside the candidate timeout,
so its ranked score contains cold-start loss. All comparative experiments must
use matched warm/warm or cold/cold arms; this cold score is an environment and
end-to-end reference.

The final score-bearing JSON, score, problem, source, binary, build stamp, and
console log were copied to `/tmp/qsb-baseline-final/` and hashed. A subsequent
read-only Nsight attempt could not access counters (`ERR_NVGPUCTRPERM`) and its
target appended records to the transient raw hit file. This did not affect the
run: `gpu_wrap.py` had already frozen 33,776 hits into `run-pinning.json`, and
the parent verifier never rereads the raw file. The final JSON contains no
duplicate tuples and independently verified every serialized hit.

For statistical decisions, the current 1% promotion bar over 235,880,780 is
238,239,588. At roughly 34,000 hits, a single 1,200-second A/B ratio has about
0.77% relative sampling noise. Short paired runs screen kernel throughput;
promotion claims require fresh holdout seeds, reversed run order, full
verification, clean power/clock telemetry, and a one-sided confidence bound
above the live bar.

## SHA-tail prototype: build and semantic validation

The prototype compiled with CUDA 12.6 for both the benchmark-shaped fat binary
and a diagnostic `sm_89` binary. Native Ada ptxas results for the production
fast path were 128 registers, a 336-byte stack frame, and one 8-byte spill load
and store. The corresponding control was 128 registers, a 528-byte stack frame,
and the same 8-byte spill load and store. Thus the specialization did not cross
the two-block occupancy boundary and reduced local stack allocation.

A matched N=24 correctness run used fresh seed `1592962926`. A two-second
attempt expired during startup and produced no work; it is discarded. With a
15-second window, the warmed control produced 417/417 verified hits at a peak
244.9 M/s. The first prototype launch produced only 225 verified hits because
the newly built fat binary paid one-time driver JIT cost inside the timeout;
those 225 tuples were an exact prefix subset of the control's hits across both
recovery IDs.

Repeating the unchanged prototype after JIT warm-up produced 441/441 verified
hits at 256.8 M/s. Every one of the control's 417 `(sequence, locktime, recid)`
tuples appeared in the prototype result, in order, and the prototype added the
next 24 valid tuples from its longer traversal. Recovery-ID counts remained
balanced (control 207/210, prototype 220/221). This gives a strong GPU semantic
check for the SHA state transition and tail-word packing. The 4.86% peak-rate
ratio is only a screen; fresh-seed, reversed-order warm runs remain necessary.

The first fresh-seed 180-second pair used seed `1942290208` in A-then-B order:

| arm | peak rate | verified hits | harness elapsed | hit-implied work | score |
| --- | ---: | ---: | ---: | ---: | ---: |
| untouched A | 244.6 M/s | 5,128 / 5,128 | 180.2 s | 43,016,781,824 | 238,686,107 |
| SHA-tail B | 256.4 M/s | 5,369 / 5,369 | 180.2 s | 45,038,436,352 | 249,925,018 |

The peak-rate ratio was 1.04824 and the noisy hit-score ratio was 1.04709.
All tuples were unique. The complete 5,128-hit A set was contained in B, with
241 additional B hits; within-batch atomic ordering differed, as expected.
Both arms sustained 100% median utilization and about 449.5 W at a fixed 450 W
limit, reached the same 72 C maximum, and had median clocks of 2,415 and
2,430 MHz respectively. Starting temperatures were 31 and 35 C. Because that
four-degree gap narrowly exceeds the predeclared three-degree cleanliness rule,
a fresh-seed reversed-order pair will use an equalized idle-floor cap before
the full holdout run.

The GPU's idle floor settled at 35 C after the first arm, so a literal 32 C
second-arm cap was unattainable. The waiting shell was stopped while the GPU
was idle, and the remaining arm was started at the measured idle floor. The
fresh-seed reverse-order pair used seed `85767983`:

| arm | peak rate | verified hits | harness elapsed | hit-implied work | score |
| --- | ---: | ---: | ---: | ---: | ---: |
| SHA-tail B, first | 256.8 M/s | 5,503 / 5,503 | 180.2 s | 46,162,509,824 | 256,155,201 |
| untouched A, second | 244.8 M/s | 5,251 / 5,251 | 180.2 s | 44,048,580,608 | 244,442,832 |

Start temperatures were 32 and 34 C. Both had a fixed 450 W limit and 100%
median utilization; median clocks were 2,430 and 2,400 MHz. All tuples were
unique, the complete 5,251-hit A set was contained in B, and B added 252 valid
hits. The peak-rate ratio was 1.04902 and the verified-score ratio was 1.04791.

Across both pairs, the peak-rate geometric mean is **1.04863** with only
0.0778 percentage points between order-specific ratios. Aggregating 10,379 A
and 10,872 B verified hits gives a hit-score ratio of 1.04751. A conservative
independent-Poisson one-sided bound is 1.02413 at 95% and 1.01459 at 99%; the
same-seed traversal actually gives positive correlation, so this model is
conservative. The predeclared full-run threshold is met.

A 1,200-second local holdout was started from the same frozen prototype and
warm table, then deliberately stopped before producing an artifact. It is not
used as evidence. The repeated opposite-order pairs already clear the decision
bound, and shorter screens provide substantially more information per GPU
minute while Yukon's isolated submission validator supplies the authoritative
long measurement. Subsequent experiments use 15-second semantic checks and
30–60-second paired throughput screens unless noise specifically requires a
longer run.

## Shared-denominator recovery experiment

The next isolated change ports `qsb_affine_finish_prepare/finish` from the
promoted sibling Subset candidate. Both tracks use byte-identical field and SHA
headers and the same homogeneous convention, `x=X/Z` and `y=Y/Z`. For affine
`R=(xR,yR)`, the helper constructs `D=xR*Z-X=Z*(xR-xP)` and
`W=Z*D=Z^2*(xR-xP)`. One inverse of W therefore yields both `1/Z` and the
affine addition denominator needed to recover `P+R` and `P-R`.

This replaces two mixed projective additions plus two-point normalization
(`25M + 4S + I`) with a shared affine finish (`10M + 3S + I`), saving 15 field
multiplies and one square per candidate. The first prototype copies the promoted
helper exactly and leaves the kernel ABI and debug path unchanged. This avoids
combining arithmetic changes with later removal of unused `neg_2u2R` inputs.

The helper has the same incomplete exceptional cases as the prior path when
`P=+/-R`, and the fixed-base routine already has an undefined zero-scalar case;
both have negligible probability for the SHA-derived input stream. Validation
requires exact control-hit containment across both recovery IDs, all-hit CPU
verification, a native resource report, and a short paired rate win.

The diagnostic native `sm_89` build used 128 registers and a 320-byte stack
frame for the production fast path, with no spill loads or stores. The generic
fallback remained separate at 544 bytes of stack and 24 bytes of spill traffic,
and the debug kernel was unchanged. An eight-second launch warmed the new fat
binary before measurement.

A 15-second same-seed semantic check used seed `1592962926`. The unchanged
frozen SHA-tail control produced 441/441 verified hits at 256.8 M/s. The
shared-denominator candidate produced 470/470 verified hits at 278.8 M/s. All
441 control tuples appeared in the candidate output, with 29 additional valid
tuples from its longer traversal. Recovery IDs remained balanced at 236/234.

A fresh 30-second paired screen used seed `1965941363`, with the candidate run
first:

| arm | peak rate | verified hits | hit-implied work | score |
| --- | ---: | ---: | ---: | ---: |
| shared-denominator candidate | 278.6 M/s | 991 / 991 | 8,383,229,871 | 275,813,164 |
| frozen SHA-tail control | 256.1 M/s | 910 / 910 | 7,710,654,108 | 253,132,974 |

The peak-rate ratio was **1.08786** and the verified-score ratio was 1.08960.
Every control tuple appeared in the candidate result, which added 81 valid
hits. Both arms began at 36 C, reached 71 C, sustained 99% utilization, and
ran against the same 450 W limit. Median power was 448.87 W versus 449.46 W;
median SM clock was 2,445 MHz versus 2,460 MHz. This is a large, clean win, so
the shared-denominator source becomes the next immutable control checkpoint.

## Promoted field-arithmetic transplant

The next experiment changes only `GPUMath.h`, retaining the frozen
shared-denominator kernel and its original 64 MiB fixed-base table. Pinning's
starting header is byte-identical to Subset's accepted `835f3b6` header. Two
later accepted Subset checkpoints form a useful attribution ladder:

- `708d476` (`ed2d86f...`) replaces the field multiplication core with its
  8-by-32-bit PTX implementation and supplies the unused XYZZ helpers;
- `4ebc4c8` (`700a8315...`) adds the dedicated 45-IMAD field square.

Testing the first header and then the second isolates the value of the
multiplier family from the specialized square. The current Subset HEAD header
is deliberately excluded because it belongs to a validation-only commit and
depends on additional include files. A header variant must compile without a
pinning source change, preserve exact hit containment at the same seed, and
show a repeatable short-run throughput gain before becoming a checkpoint.

The exact accepted `708d476` header (`ed2d86f...`) compiled as a drop-in. The
native `sm_89` fast path used 128 registers, a 352-byte stack, and 24-byte
spill loads/stores, compared with 128 registers, a 320-byte stack, and no
spills for the shared-denominator control. Despite that resource regression,
the field core was substantially faster.

After an eight-second JIT warmup, a 15-second same-seed check used seed
`1437009121`:

| arm | peak rate | verified hits | score |
| --- | ---: | ---: | ---: |
| shared-denominator control | 278.9 M/s | 455 / 455 | 252,485,304 |
| accepted 8x32 field header | 343.2 M/s | 569 / 569 | 315,037,081 |

All 455 control tuples appeared in the candidate result, which added 114
valid tuples. Recovery IDs were balanced in both arms. The peak-rate ratio
was 1.23055.

A fresh reverse-order 30-second pair used seed `2083147763`:

| arm | peak rate | verified hits | score |
| --- | ---: | ---: | ---: |
| accepted 8x32 field header, first | 343.8 M/s | 1,218 / 1,218 | 338,975,496 |
| shared-denominator control, second | 278.4 M/s | 973 / 973 | 270,946,554 |

The peak-rate ratio was **1.23491** and every one of the 973 control tuples
appeared in the candidate result, with 245 additional valid tuples. Both arms
used the fixed 450 W limit and had 99% median utilization. Candidate/control
median power was 449.04/448.62 W and median clock was 2,370/2,430 MHz; start
temperatures from metadata were 34/36 C and maxima were 69/70 C. The candidate
won even at the lower clock. This accepted header is therefore the new field
control; the next header-only test adds the accepted dedicated square.

The exact accepted `4ebc4c8` header (`700a8315...`) then added the dedicated
field square. Its native resource report was unchanged from the 8x32 control:
128 registers, a 352-byte fast-path stack, and 24-byte spill loads/stores.
After an eight-second warmup, a 15-second same-seed check used seed `265097264`:

| arm | peak rate | verified hits | score |
| --- | ---: | ---: | ---: |
| 8x32 multiply control | 343.7 M/s | 644 / 644 | 356,532,371 |
| dedicated-square candidate | 352.3 M/s | 662 / 662 | 366,513,755 |

All 644 control tuples appeared in the candidate result, with 18 additional
valid tuples. The peak-rate ratio was 1.02502.

A fresh reverse-order 30-second pair used seed `1588885998`:

| arm | peak rate | verified hits | score |
| --- | ---: | ---: | ---: |
| dedicated-square candidate, first | 352.9 M/s | 1,218 / 1,218 | 339,017,760 |
| 8x32 multiply control, second | 343.5 M/s | 1,193 / 1,193 | 332,173,690 |

The peak-rate ratio was **1.02737** and all 1,193 control tuples appeared in
the candidate result, with 25 additional valid tuples. Both arms had identical
2,370 MHz median clocks, 99% utilization, 69 C maxima, and about 449 W median
power at the fixed 450 W limit. The accepted dedicated-square increment is a
stable win and becomes the next immutable checkpoint.

## Block-wide inverse experiment

The next isolated change replaces the per-thread inverse of the shared affine
denominator with one product-tree inverse per 256-thread block. A prior
warp-only Pinning attempt still issued one inversion stream per warp; the
two-level block tree reduces eight warp inversion streams to one, at the cost
of field products, shuffles, two barriers, and 2,560 bytes of shared memory.

The first test uses the latest carry-preserving `qsb_field_mul` plus exact
`qsb_warp_inverse` and `qsb_block_inverse` control flow from the sibling
candidate. Tail blocks keep all threads alive through the collectives and use
one as the inactive lanes' factor. Entirely empty blocks return uniformly.
Correctness requires every active lane to receive its own inverse, exact
same-seed hit containment, full CPU verification, and no barrier divergence.
The inherited zero-denominator exceptional cases are vanishingly rare in the
benchmark stream and are recorded separately from this performance test.

The native `sm_89` fast path compiled at 128 registers, a 368-byte stack,
2,560 bytes of shared memory, 108-byte spill stores, and 52-byte spill loads.
The prior field checkpoint used a 352-byte stack and 24-byte spill loads/stores.
The collective therefore increases spill traffic, but remained comfortably
within the same two-block occupancy target.

After an eight-second warmup, a 15-second same-seed semantic check used seed
`331888972`:

| arm | peak rate | verified hits | score |
| --- | ---: | ---: | ---: |
| dedicated-square control | 352.4 M/s | 628 / 628 | 348,144,404 |
| block-wide inverse | 415.4 M/s | 743 / 743 | 411,291,579 |

All 628 control tuples appeared in the candidate result, with 115 additional
valid tuples. The peak-rate ratio was 1.17877.

A fresh reverse-order 30-second pair used seed `776702128`:

| arm | peak rate | verified hits | score |
| --- | ---: | ---: | ---: |
| block-wide inverse, first | 415.9 M/s | 1,397 / 1,397 | 388,710,340 |
| dedicated-square control, second | 352.0 M/s | 1,184 / 1,184 | 329,606,039 |

The peak-rate ratio was **1.18153** and all 1,184 control tuples appeared in
the candidate result, with 213 additional valid tuples. Both arms sustained
99% utilization near 449 W at the fixed 450 W limit. Both began active work
at 62 C; candidate/control maxima were 69/70 C and median clocks were
2,445/2,370 MHz. The gain is much larger than the clock difference and agrees
with the opposite-order semantic screen.

Before this checkpoint is used as a control, a small safety delta will mask an
active zero denominator to field identity during the collective and suppress
only that singular lane afterward. This prevents a probability-negligible
exception from poisoning its 255 neighbors. The safety delta gets a separate
resource and short-rate check because its four-limb zero predicate is on the
common path.

The zero-denominator mask improved the native fast-path resource report to a
352-byte stack with 28-byte spill loads/stores, from 368 bytes and 108/52-byte
spill stores/loads before the mask. It retained 128 registers and 2,560 bytes
of shared memory. This unexpected improvement comes from returning unusable
lanes immediately after the collective, shortening later live ranges.

After an eight-second warmup, a 15-second same-seed comparison used seed
`629663757`. The pre-mask control ran at 416.2 M/s with 697/697 verified hits
and score 385,617,726. The safe candidate ran at 417.3 M/s with 702/702 hits
and score 388,672,781. All 697 control tuples appeared in the candidate result.
The peak-rate ratio was 1.00264. The safety check is performance-neutral within
the short-run resolution and materially strengthens the collective failure
boundary, so it is retained.

## Direct SHA-state consumption

The next small experiment removes two byte round-trips from the ranked fast
path. SHA-256 state words are already the big-endian digest words: the scalar
limbs can be assembled directly from `s2`, and the N-bit gate can test the high
bits of `hs[0..]` without materializing a 32-byte digest. In single-hash mode,
the second-hash padded block is also skipped before its byte buffer is built.

The generic and easy diagnostic paths retain byte construction where their
interfaces require it. The direct scalar limb order and word gate are copied
from the sibling implementation and independently checked against the prior
byte loops. The performance gate is the same 15-second containment check and a
30-second fresh-seed pair only if the resource report and first rate are sound.

A CPU audit compared 100,000 random SHA states against the old scalar byte
loop and checked the word gate for every N from 1 through 256; all cases
matched. The existing tail audit also retained all 2,320 SHA256d comparisons.
The native fast-path resource report was unchanged at 128 registers, a
352-byte stack, 28-byte spill loads/stores, and 2,560 bytes of shared memory.

After an eight-second warmup, a 15-second same-seed comparison used seed
`1364578372`. The zero-safe control ran at 417.6 M/s with 769/769 verified
hits and score 426,488,491. The direct-state candidate ran at 417.9 M/s with
770/770 hits and score 426,491,833. All 769 control tuples appeared in the
candidate result. The peak-rate ratio was only 1.00072, below useful standalone
resolution, so no 30-second pair was spent on it. The change is retained as a
verified neutral cleanup and because direct scalar words simplify the later
folded-base experiment; it is not claimed as a performance improvement.

## Signed-table XYZZ fixed-base experiment

The next major experiment ports the accepted `6d97bb4` fixed-base path while
retaining the current SHA frontend, scalar multiplication, zero-safe block
inverse, shared affine recovery, and accepted field header. It recodes the
already reduced `u1` into 16 nonzero signed odd digits, halves the affine table
from 64 MiB to 32 MiB, and accumulates table points in XYZZ coordinates before
one conversion to the homogeneous interface.

The table remains problem-independent G/2 in this stage. Host-generated 512 KiB
ladders feed a parallel GPU table builder, whose output is spot-checked against
OpenSSL before search. Folding `neg_r_inv` into the table is deliberately a
later experiment so signed geometry and removal of per-candidate scalar
multiplication remain separately measurable.

The accepted incomplete XYZZ addition has four exceptional effective scalar
residues. The production zero-denominator mask prevents any resulting infinity
from poisoning the block, but initially suppresses the singular lane. A strict
exception repair and targeted coordinate audit are tracked separately; random
encounter probability over a benchmark run is effectively zero. Ordinary
validation still requires exact same-seed hit containment and full CPU
verification before performance evidence is considered.

The completed signed-table source has SHA-256
`d2bbf43d9d47ebda4e6a3e35b0b6d3e1e1fa24b0c84693fcbdc77dd2130bcf59`.
Its native Ada fast kernel retained 128 registers and 2,560 bytes of shared
memory. The stack grew from 352 to 384 bytes; spill loads/stores grew from
28/28 to 36/40 bytes. The builder itself uses 128 registers, a 120-byte stack,
and no spills.

The first cold eight-second run used seed `741231449`. It built the host
ladders and GPU table inside the external timeout, then completed one full
sequence and part of the next. Its 177 verified tuples were an exact subset of
the direct-state control, including an exact 142-of-142 match for the fully
completed first sequence. The harness nevertheless extrapolated the post-start
535.3 M/s rate across cold table construction, overreported candidates, and
printed a Poisson warning. This is a short-timeout accounting artifact rather
than a semantic failure. It also demonstrates that cold setup time must be
recorded separately from steady-state rate.

A warm 15-second same-seed comparison used seed `1827992113`:

| arm | peak rate | verified hits | score |
| --- | ---: | ---: | ---: |
| direct-state control | 418.6 M/s | 729 / 729 | 403,790,365 |
| signed-table XYZZ | 533.5 M/s | 937 / 937 | 519,035,700 |

All 729 control tuples appeared in the candidate result, the candidate added
208 valid tuples, and neither arm emitted duplicates. The peak-rate ratio was
**1.27449** and the hit-derived score ratio was **1.28541**. This margin is
decisive despite the modest spill increase, so a redundant 30-second pair was
skipped to preserve experiment throughput. The next isolated delta folds
`neg_r_inv` into the table base and recodes reduced raw `z`, deleting the
per-candidate scalar multiplication.

## Folded problem-specific fixed base

This isolated delta folds `neg_r_inv` into base `A = neg_r_inv*G`, builds the
signed table on `A/2`, reduces raw `z` once modulo the group order, and recodes
`z` directly. It removes the full 256-bit `gpu_scalar_mulmod` from each
candidate. Both old G-based caches are removed because the new table depends
on the generated problem. A 100,006-case CPU audit, including `0`, `n`,
`n+1`, and `2^256-1`, confirmed the signed digits reconstruct `2z mod n` and
remain odd, nonzero, and within the table range.

The folded source has SHA-256
`2ba35cf6502793824fc694ac8896a36875f3b7724342888074a6978e85dfb2e2`.
The fast kernel's native resource report is unchanged from the signed-table
control: 128 registers, a 384-byte stack, 36-byte spill loads, 40-byte spill
stores, and 2,560 bytes shared. A one-point process measured the complete
problem-specific ladder and GPU-table setup at 0.28 seconds after JIT warmup.

The first 15-second pair used seed `1270042475`. Its first process paid the
one-time CUDA PTX JIT cost, so hit-based scoring covered only part of the
claimed extrapolated work. Steady-state peak rates were 535.3 M/s for the
signed control and 540.1 M/s for the fold, ratio 1.00897. All 651 tuples reached
by the folded run were exact members of the farther-running control result.

After a one-point JIT warmup, a reverse-order 15-second pair used seed
`874181770`:

| arm | peak rate | verified hits | score |
| --- | ---: | ---: | ---: |
| folded A/2 table, first | 539.9 M/s | 920 / 920 | 510,191,623 |
| signed G/2 control, second | 533.2 M/s | 915 / 915 | 507,281,440 |

All 915 control tuples appeared in the fold result, with five additional valid
tuples and no duplicates. The second peak-rate ratio was 1.01257; the geometric
mean across the two orders was **1.01076**, with neither ratio below 1.005.
The 0.28-second cold table construction costs about 0.023% of a 1,200-second
official run, so the fold is retained as a net improvement.

## Rejected streamed-digit variant

A follow-up consumed each signed digit directly from the four-limb recoder
state instead of materializing the 16-entry digit array. Native Ada compilation
reduced the fast-path stack from 384 to 320 bytes but increased spill
loads/stores from 36/40 to 52/68 bytes, with the same 128 registers and 2,560
bytes shared.

After a one-point JIT warmup, a 15-second pair used seed `420437549`. The
streamed variant ran at 541.4 M/s with 889/889 verified hits; the folded-array
control ran at 539.9 M/s with 885/885 hits. All 885 control tuples appeared in
the streamed result and neither arm emitted duplicates. The rate ratio was
only 1.00278, below the 0.5% screening threshold, while spill traffic worsened.
The experiment is therefore rejected and the exact folded-array source is
restored.

## Rejected alternate symmetric square

The validation-only `square32.cuh` helper from commit `90072ea` was tested
against the accepted 4eb dedicated square. The imported helper matched its
historical SHA-256 `96cff841d26b3fdf86062d083ce72ef25e2d3c3176018358eaa961ed81236d41`.
Native Ada compilation kept 128 registers and 2,560 bytes shared, but changed
the fast stack from 384 to 400 bytes and spill loads/stores from 36/40 to
36/36 bytes.

After a one-point JIT warmup, a 15-second comparison used seed `1396898522`.
The alternate square ran at 536.7 M/s with 877/877 verified hits and score
485,523,162. The folded control ran at 540.0 M/s with 883/883 hits and score
489,541,900. The rate ratio was 0.99389, a clear regression, so the alternate
helper and header integration were removed and the accepted 4eb header was
restored byte-for-byte.

## Intermediate 16-bit digit array

The signed fixed-base recode was encoded as sixteen 16-bit `(sign,index)`
codes instead of sixteen 32-bit signed digits. A 100,006-scalar CPU audit,
including reduction boundaries around the group order, matched every reference
digit and reconstructed `2 * (z mod n) mod n` exactly. Native Ada compilation
reduced the production kernel stack from 384 to 352 bytes while retaining 128
registers, 36-byte spill loads, 40-byte spill stores, and 2,560 bytes shared.

After a one-point JIT warmup, a 15-second pair used seed `1947283103`. The
16-bit array ran at 542.4 M/s with 934/934 verified hits; the folded control ran
at 540.7 M/s with 933/933 verified hits. All 933 control tuples appeared in the
candidate result, with one additional tuple and no duplicates. The self-rate
ratio was 1.00314, below the 0.5% screening threshold, so this representation
was kept only as an intermediate toward scalar packed words rather than
accepted on its own.

## Rejected four-word packed digits

The sixteen 16-bit codes were next packed into four by-value 64-bit words so
the production multiply could consume them without dynamically indexing an
array. A 10,006-scalar CPU audit matched the array codes and reconstructed the
same reduced scalar. Native Ada compilation reduced the production stack to
320 bytes, but spill loads/stores worsened to 56/56 bytes from the folded
control's 36/40. This repeats the streamed-recorder pressure tradeoff, so the
variant was rejected at the compiler gate without spending a timed run.

## Accepted fused signed-table negation

The signed table loader originally materialized both `y` and `p-y`, then
selected four limbs with a sign mask. The accepted form uses that mask to
compute either `y` or `~y + (p+1)` directly through one carry chain. A
200,012-case limb audit over edge and random inputs matched the old
representation exactly for both signs, including its noncanonical `y=0`
behavior. Native Ada resources stayed at 128 registers, a 384-byte production
stack, 36-byte spill loads, 40-byte spill stores, and 2,560 bytes shared.

After a one-point JIT warmup, two reverse-order 8-second pairs used seed
`602941883`:

| order | fused rate | control rate | ratio | fused hits | control hits |
| --- | ---: | ---: | ---: | ---: | ---: |
| fused first | 544.5 M/s | 539.3 M/s | 1.00964 | 492 / 492 | 489 / 489 |
| fused second | 543.4 M/s | 540.5 M/s | 1.00537 | 491 / 491 | 489 / 489 |

The geometric-mean rate ratio was **1.00750**, and neither order fell below
the 0.5% screen. In both pairs every control tuple appeared in the fused arm;
the fused arm added three and two tuples respectively, covered both recovery
IDs, and emitted no duplicates. The fused loader is retained.

## Accepted parity-only affine outputs

The production hash path consumes recovered y coordinates only through their
least-significant bits for compressed-public-key prefixes. The affine finish
now returns those two parity bits and retains only the two x coordinates. Its
existing scratch is reused for each y expression, and the final Q2 field
negation is replaced by parity inversion because the secp256k1 prime is odd.
The full-coordinate debug path remains unchanged. A 100,006-case
boundary/random audit confirmed that `(p-a) mod 2^256` has parity
`1 ^ parity(a)` for every tested representation.

Native Ada compilation kept 128 registers, 36-byte spill loads, 40-byte spill
stores, and 2,560 bytes shared while reducing the production stack from 384 to
304 bytes. After a one-point JIT warmup, two reverse-order 8-second pairs used
seed `1109384721`:

| order | parity rate | fused control | ratio | parity hits | control hits |
| --- | ---: | ---: | ---: | ---: | ---: |
| parity first | 548.0 M/s | 544.2 M/s | 1.00698 | 473 / 473 | 467 / 467 |
| parity second | 546.8 M/s | 543.4 M/s | 1.00626 | 472 / 472 | 467 / 467 |

The geometric-mean rate ratio was **1.00662**, with both orders above the 0.5%
screen. Every control tuple appeared in its candidate arm; the candidate arms
added six and five tuples, covered both recovery IDs, and emitted no
duplicates. The parity-only finish is retained.

## Rejected 512-thread blocks

The search kernel's block size was doubled from 256 to 512 while changing its
launch bound from two resident blocks to one. Existing collective storage and
tail identity substitution already support sixteen warps, and native Ada
resources remained unchanged at 128 registers, a 304-byte production stack,
36-byte spill loads, 40-byte spill stores, and 2,560 bytes shared. The shape
halves block inversions and reduces tree work from 5,504 field multiplications
across two 256-thread blocks to 5,376 per 512-thread block.

A JIT-warmed 4-second pair used seed `1640325937`. The 512-thread arm ran at
508.9 M/s with 212/212 verified hits; the 256-thread parity control ran at
548.0 M/s with 232/232 verified hits. All 212 candidate tuples appeared in the
control and neither arm emitted duplicates. The rate ratio was 0.92865, showing
that loss of inter-block latency hiding overwhelms the cheaper collective. The
two launch lines were reverted exactly without a reverse run.

## Rejected explicit fast-tail SHA gates

The benchmark-only specialization was changed from a two-iteration generic
public-key loop to two explicit inline calls, one for each recovery ID. The
general easy/double-hash path remained unchanged. Native Ada compilation kept
128 registers, 36-byte spill loads, 40-byte spill stores, and 2,560 bytes
shared while reducing the fast production stack from 304 to 224 bytes.

After a one-point JIT warmup, two reverse-order 4-second pairs used seed
`207391684`:

| order | explicit rate | parity control | ratio | explicit hits | control hits |
| --- | ---: | ---: | ---: | ---: | ---: |
| explicit first | 549.7 M/s | 547.4 M/s | 1.00420 | 224 / 224 | 223 / 223 |
| explicit second | 549.3 M/s | 548.1 M/s | 1.00219 | 224 / 224 | 223 / 223 |

Every control tuple appeared in its candidate arm, each candidate added one
tuple, both recovery IDs were represented, and neither arm emitted duplicates.
The geometric-mean rate ratio was 1.00320, below the 0.5% screen. The explicit
gates were rejected despite lower stack use; their larger inlined SHA footprint
did not produce a material throughput gain, and the parity checkpoint was
restored byte-for-byte.

## Accepted direct XYZZ recovery

The fixed-base multiplier now exposes its raw `X,Y,ZZ,ZZZ` accumulator to the
production recovery path; the independent debug path retains the old
three-multiplication homogeneous conversion. With `d=xR*ZZ-X`, the production
path collectively inverts `ZZ^2*d` and derives both slopes, x coordinates, and
y parities while anchoring the y formulas at the fixed point R. This changes
the recovery tail from 13 field multiplications plus three squares to ten field
multiplications plus four squares. A 10,000-pair formula audit and an
independent 2,000-pair scaled-curve audit matched reference `P+R` and `P-R`
x/parity outputs for every nonsingular case tested. The singular set and
identity-lane collective behavior are unchanged.

Native Ada compilation preserved the accepted allocation exactly: 128
registers, a 304-byte production stack, 36-byte spill loads, 40-byte spill
stores, and 2,560 bytes shared. After a one-point JIT warmup, two reverse-order
8-second pairs used seed `931648207`:

| order | direct XYZZ | parity control | ratio | XYZZ hits | control hits |
| --- | ---: | ---: | ---: | ---: | ---: |
| XYZZ first | 553.1 M/s | 547.1 M/s | 1.01097 | 512 / 512 | 507 / 507 |
| XYZZ second | 551.3 M/s | 547.4 M/s | 1.00712 | 508 / 508 | 505 / 505 |

The geometric-mean rate ratio was **1.00904**, with both orders above the 0.5%
screen. Every control tuple appeared in its candidate arm; the candidate arms
added five and three tuples, covered both recovery IDs, and emitted no
duplicates. Direct XYZZ recovery is retained.

## Accepted 16M host batches

The search loop originally synchronized the device and copied its hit counter
after every 1,048,576 candidates, only about 1.9 ms of kernel work at the new
throughput. A temporary runtime override isolated 8M, 16M, and 32M batches on
the exact direct-XYZZ kernel. Four-second screens against stable 1M controls
measured 562.9, 564.1, and 564.4 M/s respectively, versus 552.4--552.6 M/s.
The 16M point captures nearly the full plateau while keeping half the kernel
latency of 32M, so the override was removed and 16,777,216 was made the fixed
default.

Two reverse-order 4-second comparisons used seed `1482057193`:

| order | 16M batch | 1M control | ratio | 16M hits | control hits |
| --- | ---: | ---: | ---: | ---: | ---: |
| 16M first | 564.1 M/s | 552.4 M/s | 1.02118 | 253 / 253 | 243 / 243 |
| 16M second | 563.3 M/s | 552.8 M/s | 1.01899 | 249 / 249 | 246 / 246 |

The geometric-mean rate ratio was **1.02009**. Every control tuple appeared in
its 16M arm; the candidate arms added ten and three tuples, covered both
recovery IDs, and emitted no duplicates. The larger grid does not change
kernel arithmetic or collective shape; indices remain below the 30-bit hit-tag
boundary, and the expected hits per batch remain far below the 1,024-entry
device buffer.

## Fixed-block SHA compiler audit

Native Ada SASS inspection showed that whole-program compilation already
inlines the fixed 33-byte public-key SHA transform, propagates its padding and
length constants, scalarizes the schedule without local-memory traffic, drops
unused digest feed-forwards, and emits the 24-bit predicate directly. There is
one rolled SHA body for both recovery IDs. A manual source specialization would
therefore reproduce existing code at best and risk a larger instruction
footprint, so it was rejected without editing or timing.

## Accepted level-packed shared inverse tree

The block-wide denominator inversion previously built a redundant all-reduce
inside each warp and then repeated the pattern over the eight warp products.
Counting the stride-one copies correctly, that implementation executed 2,752
field multiplications and 86 warp-level multiplication sequences per 256-lane
block. The replacement builds one level-packed binary product tree in shared
memory, inverts its root once, and propagates inverses back to all leaves using
`(L*R)^-1*R=L^-1` and `(L*R)^-1*L=R^-1`.

Each tree level stores its left half before its right half in limb-major order.
This gives contiguous operands across active lanes and makes a sibling a simple
half-level XOR. The immutable product tree occupies 16 KiB and the internal
inverse levels occupy 8 KiB. The resulting collective uses 765 field
multiplications, 31 warp-level multiplication sequences, 16 CTA barriers, and
one inversion. Inactive tail lanes and the fixed singular cases still enter as
multiplicative identities, so every thread reaches every barrier.

`audit_shared_tree.py` checked 211 full 256-value blocks modulo the secp256k1
prime, including all-identity tails, active counts around warp and half-block
boundaries, zero replacement, field edges, and random mixtures. Every returned
leaf multiplied by its effective input to one. A three-second GPU smoke then
verified 170/170 emitted hits and passed the benchmark variance check.

Native Ada compilation retained 128 registers and the 304-byte fast-kernel
stack. Fast-path per-thread spills improved from 40-byte stores / 36-byte loads
to 24/24, while shared memory rose from 2,560 to 24,576 bytes. Two 256-thread
blocks still fit: registers remain the limiting resource. The root inversion,
executed by only one lane per block, reports 28-byte stores / 32-byte loads in
its helper; its amortized cost is negligible relative to the eliminated field
multiplications.

After JIT warmup, two reverse-order four-second comparisons used seed
`1700005678`:

| order | packed tree | 16M XYZZ control | ratio | tree hits | control hits |
| --- | ---: | ---: | ---: | ---: | ---: |
| packed tree first | 569.2 M/s | 563.0 M/s | 1.01101 | 248 / 248 | 246 / 246 |
| packed tree second | 569.2 M/s | 563.5 M/s | 1.01012 | 248 / 248 | 246 / 246 |

The geometric-mean rate ratio was **1.01056**, and both orders exceeded the
0.5% acceptance screen. In each pair every control tuple appeared in the tree
result, the tree added two later-frontier tuples, both recovery IDs were
represented, and neither arm emitted duplicates. The packed inverse tree is
retained. Its tested source SHA-256 is
`10a3151455c94431b1b893b17d025c196c2015726abc2c8c8ead7b46bcf8e31e`.

## Rejected mixed warp/CTA synchronization

The packed tree was prototyped with block barriers only at levels where work
passes to another warp, using warp barriers for the remaining levels. This
would reduce the collective from sixteen CTA barriers to six without changing
its 765 field multiplications or shared layout. Native compilation retained
128 registers, the 304-byte fast stack, 24/24-byte caller spills, and 24,576
bytes shared, although the lane-zero inversion helper's spills rose from
28/32-byte stores/loads to 36/40.

A two-second smoke completed no batch and emitted no hits, consistent with a
cross-warp synchronization hazard in the mixed-barrier schedule. It was
rejected immediately without an A/B run. The fully synchronized sixteen-barrier
tree was retained byte-for-byte. Future barrier reductions need a standalone
one-block CUDA collective test before entering the benchmark harness.

## Accepted constant-memory fixed point

The recovery tail read the same affine `u2R` x coordinate twice and its y
coordinate once per candidate through twelve 64-bit global loads. These values
are fixed for the loaded problem. Two four-limb constant-memory symbols now
receive byte-identical copies from the existing problem arrays before any
kernel launch. Both production templates read the symbols; the independent
debug kernel and its existing global pointers remain unchanged.

On the packed-tree control, native Ada compilation kept 128 registers and
24,576 bytes shared while reducing the fast stack from 304 to 288 bytes and
eliminating its 24-byte spill loads and stores. The lane-zero inversion
helper's contextual spills fell from 32/28-byte loads/stores to 16/16. Global
loads in the fast kernel fell from 32 to 20, while the constant-bank footprint
rose from 892 to 960 bytes. Static code grew by 48 instructions because the
compiler folded constant limbs into arithmetic differently, so the change was
timed rather than accepted solely from allocation.

An initial two-second probe verified all 94 emitted hits but did not reach the
harness's 100-hit variance threshold; it is correctness evidence only. After
that warmup, two reverse-order three-second comparisons used seed `1700007890`:

| order | constant u2R | packed-tree control | ratio | constant hits | control hits |
| --- | ---: | ---: | ---: | ---: | ---: |
| constant first | 575.1 M/s | 569.8 M/s | 1.00930 | 163 / 163 | 161 / 161 |
| constant second | 574.2 M/s | 569.3 M/s | 1.00861 | 163 / 163 | 161 / 161 |

The geometric-mean rate ratio was **1.00895**, with both orders above the 0.5%
screen. Every control tuple appeared in its candidate arm, each candidate added
two later-frontier tuples, both recovery IDs were present, and no duplicates
were emitted. Constant-memory `u2R` is retained. Its tested source SHA-256 is
`a2d4a95f78e7a1f0d42207ae1e1ce1f417278703612aa74004e77cb3fcdf2a75`.

## Rejected 128-thread packed tree

The packed collective was specialized from 256 to 128 leaves, its shared
storage reduced from 24,576 to 12,288 bytes, and the launch bound changed to
four 128-thread blocks. This preserves sixteen resident warps while providing
four independently schedulable CTAs to hide barriers. It also doubles the
number of root inversions per candidate range and raises the tree's scheduled
field-multiply sequences from 31 per 256 inputs to 38 across two 128-input
blocks. A 250-block modular audit matched every leaf inverse.

Native Ada compilation retained 128 registers, the 288-byte fast stack, zero
caller spills, and the constant variant's 16/16-byte lane-zero inversion-helper
spills. A three-second same-seed screen measured 574.3 M/s for 128 threads and
574.5 M/s for the accepted 256-thread control, ratio **0.99965**. The candidate
verified 172/172 unique hits and the control 173/173; the control's sole extra
tuple was beyond the candidate's last completed batch when timeout interrupted
the runs. The speed ratio was far below the 0.5% gate, so the experiment was
rejected without a reverse run and the 256-thread tree retained.

## Rejected constant-memory per-sequence SHA state

The fast kernel's eight shared midstate words were moved from a read-only global
pointer to an eight-word constant symbol updated once per sequence. The generic
and debug paths retained their original pointer. Native Ada compilation kept
the fast kernel at 128 registers, a 288-byte stack, zero caller spills, 24,576
bytes shared, and the same 10,520 static instructions. It removed eight scalar
global loads and increased the constant-bank footprint by exactly 32 bytes.

After warmup, a three-second same-seed screen used seed `1700010123`. The
constant-midstate candidate ran at 575.9 M/s and the accepted control at 574.1
M/s, ratio **1.00314**. Both arms verified the same 175 unique tuples exactly,
covered both recovery IDs, and emitted no duplicates. The gain was below the
0.5% acceptance threshold, so the change was rejected without a reverse run.
The existing read-only global midstate path is retained.

## Short-run policy after the constant-point checkpoint

The accepted kernel is now fast enough that long exploratory runs have poor
information value.  New experiments use a staged gate:

1. Prove the changed algebra, address map, and output tags independently on the
   CPU, including boundary values and inactive collective lanes.
2. Compile for native `sm_89` and reject resource regressions before timing.
   The current checkpoint is 128 registers, a 288-byte fast-kernel stack, zero
   fast-caller spills, and 24,576 bytes of shared memory.
3. Warm the JIT, then use a same-seed 2--4 second screen.  Each arm must verify
   at least 100 hits, cover both recovery IDs, emit no duplicates, and contain
   exactly the control tuples over the common completed frontier.
4. Run the reverse order only when the first ratio reaches 1.005.  Retain a
   change only when both orders and their geometric mean clear that threshold.

This policy concentrates GPU time on decisions.  Correct-but-neutral changes
are recorded and reverted instead of being accumulated into the production
source.

## Literature-guided 15-term table design

The regular odd recoding in this candidate is the Joye--Tunstall recurrence:
choose an odd representative, take
`e = (M mod 2^(w+1)) - 2^w`, and advance by
`M = (M-e)/2^w = 2*floor(M/2^(w+1))+1`.  This is the regular signed-digit
construction described in *Fast and Regular Algorithms for Scalar
Multiplication* ([ePrint 2011/338](https://eprint.iacr.org/2011/338.pdf)) and
subsequent regular-recoding work
([ePrint 2015/036](https://eprint.iacr.org/2015/036.pdf)).

The current sixteen width-16 terms require `16 * 2^15 = 524,288` affine
points, or 32 MiB for x and y.  Removing one mixed addition requires fifteen
digit widths whose sum covers 256 scalar bits.  Convexity of the per-window
cost `2^(w-1)` makes the balanced integer split minimal: fourteen width-17
terms and one width-18 top term.  That design has
`14 * 2^16 + 2^17 = 1,048,576` points, exactly 64 MiB of coordinates.  It saves
one 8M+2S XYZZ mixed addition and four 128-bit coordinate loads per candidate,
at the cost of doubling the random table footprint.  A complete local address
and recoding audit is the mandatory first gate; native resource and short A/B
results decide whether the 4090's L2 can retain enough of the larger table.

## Table-load cache-policy probe

The remaining fixed-base loop performs sixteen random point reads: 64
128-bit global loads, or 1,024 logical bytes per candidate.  NVIDIA documents
L1 as per-SM and L2 as device-wide, and documents `__ldcg` as a load using the
corresponding cache operator for CUDA vector types
([CUDA C++ Programming Guide](https://docs.nvidia.com/cuda/cuda-programming-guide/),
[CUDA 12.5 load-function reference](https://docs.nvidia.com/cuda/archive/12.5.0/cuda-c-programming-guide/index.html)).
Because this access stream is random over a shared 32 MiB table, bypassing L1
may avoid overfetch and eviction of shorter-lived data while preserving L2
reuse across SMs.  The isolated probe changes only the four `ulonglong2` point
loads to `__ldcg`; its address audit passes.  The compile gate requires the
accepted register, stack, spill, and shared-memory allocation, plus confirmation
of the intended global cache operator in SASS.  Its expected gain is small, so
one forward 3-second run is enough to reject it below 1.005.

A direct Nsight Compute sample of the accepted binary was also attempted with
a 45-second cap.  The host denied access to NVIDIA performance counters with
`ERR_NVGPUCTRPERM`; no counter values were collected.  Rather than spend more
time on unavailable telemetry, the experiment queue uses static SASS/resource
inspection followed by short controlled A/B runs.

## Prepared synchronization and encoding probes

Two other local probes passed their non-GPU gates and remain outside the
production source:

- A conservative packed inverse-tree schedule retains CTA barriers only for
  actual cross-warp publications.  It has seven dynamic CTA barriers and nine
  unconditional full-mask warp barriers, versus sixteen CTA barriers in the
  accepted tree.  A symbolic proof covers every shared producer/consumer edge,
  and the same 211 modular cases pass.  It must pass one-block `synccheck` and
  `racecheck` before benchmark timing.
- A compact signed-digit encoding stores `(abs(e)-1)/2` in bits 0--14 and the
  sign in bit 15.  An exhaustive digit-domain and 200,152-scalar reconstruction
  audit passes.  The point lookup is unchanged; the purpose is to test whether
  halving the materialized digit footprint lowers the 288-byte stack without
  reintroducing spills.

The explicit two-call fast public-key SHA gate was reconstructed against the
latest source and passed 200,518 packing/tag cases, but it is deliberately
low priority.  The same source shape previously reduced stack by 80 bytes yet
measured only 1.00320 geometric-mean gain because duplicating the inlined SHA
body enlarged the instruction footprint.

## Ranked calibration: packed tree alone

The level-packed tree was submitted by itself so ranked validation could test
whether the short local signal survived the official 1,200-second run.  It
scored **552,343,391 verified candidates/s** against the promoted
**549,286,075** checkpoint: an absolute gain of 3,057,316 and a direct relative
gain of **0.5566%**.  Yukon marked the submission rejected, so the faster result
was not promoted.  The manifest sets `minScoreImprovementBips` to 100, an exact
1% promotion threshold, so the outcome follows directly from that rule.

This result is still valuable calibration.  The local reverse-order geometric
mean was 1.01056, roughly twice the ranked uplift.  The 0.5% local gate remains
useful for deciding whether an isolated change is real enough to keep, but a
single small change is no longer submitted immediately.  Small retained wins
are bundled until their local cumulative margin over the promoted source is at
least 1.25%, with preference for 1.5% or more.  Large architectural changes
continue to receive their own submissions because their validator result is
high-information even when they miss promotion.

## Promoted constant-table checkpoint

The next ranked submission kept the level-packed inverse tree and moved the
problem-specific `u2R` point into CUDA constant memory.  Yukon promoted it at
**557,704,499 verified candidates/s**, an absolute gain of 8,418,424 and a
direct relative gain of **1.5326%** over the previous 549,286,075 checkpoint.
This also confirms that the earlier packed-tree gain was real even though its
isolated 0.5566% improvement did not clear the promotion margin.  The promoted
submission id is `0c4673b4-8a5d-4ce4-88ec-38e0faaa8ce8`; Yukon recorded commit
`bf30948`.

The current 15-term table experiment is intentionally submitted separately.
That keeps its 64 MiB table-footprint tradeoff observable in the ranked result
instead of hiding it inside unrelated instruction-scheduling and cache-policy
changes.

At this frontier the next promotion requires at least **563,281,544** verified
candidates/s.  This exact cutoff reinforces the experiment policy: isolated
sub-percent results remain useful measurements, while promotion-oriented
bundles should target enough cumulative margin to clear 1% despite run noise.

## Ranked result: 15-term table

The 15-term nonuniform table scored **550,660,977 verified candidates/s** and
was rejected.  It is 7,043,522, or **1.2629%**, below the promoted
557,704,499 constant-table checkpoint.  It remains 1,374,902, or 0.2503%, above
the earlier 549,286,075 checkpoint, but that comparison omits the constant
`u2R` gain already present in its source and does not make the geometry
competitive.

This resolves the central tradeoff: on the target RTX 4090, removing one mixed
XYZZ addition and four 128-bit coordinate loads does not repay doubling the
random table footprint from 32 MiB to 64 MiB.  Further work returns to the
promoted 16-term geometry.  The nonuniform recoder and its exhaustive audit are
retained here as a reproducible negative result, not as the production base.

## Native-code triage for the next short runs

CUDA 12.6 `sm_89` builds and `cuobjdump`/`nvdisasm` inspection provide a cheap
gate before consuming RTX 4090 time.  The accepted-width control's fast kernel
uses 128 registers, a 288-byte stack, no caller spills, 24,576 bytes of shared
memory, and 10,520 encoded instruction slots.  The strongest isolated static
candidate simply reschedules the existing XYZZ mixed-add operations so that
`PP`, `PPP`, and the original `Y1` die earlier.  It preserves the formulas and
operation count while reducing the fast kernel by 32 instruction slots, two
local loads, two local stores, and 16 stack bytes.  It also removes the
contextual 16-byte `_ModInv` spill loads and stores.  A separate OpenSSL-backed
group audit passed 10,000 arbitrary additions and 300 accumulated points.

Other prepared probes rank below that schedule for first-pass timing:

- `__ldcg` table reads add 16 slots but reduce two local loads, two local
  stores, and 16 stack bytes; the generated PTX contains 36 `.cg` global-load
  instructions across all kernels, while the fast kernel contains 12 such PTX
  loads and 12 matching cache-qualified native loads.
- The compact 16-bit digit representation saves 32 stack bytes but adds 120
  instruction slots.  That is a poor trade without measured local-memory
  pressure.
- The conservative seven-CTA-barrier inverse tree adds 16 slots and seven
  branch instructions.  Its value depends on barrier latency and therefore
  still requires sanitizer plus runtime evidence.
- The explicit two-call SHA gate saves 96 stack bytes but adds 1,288 slots, a
  12.24% code-size increase that explains its low priority.

These counts are static occurrence counts, not executed-instruction counts;
unrolled and predicated paths can make their runtime weights differ.  They are
used to order 2--4 second A/B screens rather than to claim throughput gains.

## L2 persistence probe

An isolated host-side probe places the contiguous X/Y table in one CUDA access
policy window and requests the device's effective persisting-L2 set-aside.  On
the development RTX 4090, the queried L2 size is 72 MiB, the maximum persisting
set-aside is 49.5 MiB, and the maximum access-policy window is approximately
128 MiB.  The resulting hit ratio is 1.0 for the accepted 32 MiB table and
0.7734375 for the 64 MiB 15-term table.  CUDA treats this policy as a hint, so
those numbers establish only that the experiment is valid; a short A/B run
must decide whether it helps.

The reviewed probe uses the effective limit returned by `cudaDeviceGetLimit`,
keeps uploads, counter resets, kernels, and readbacks on the same explicit
stream, and synchronizes setup before benchmark timing.  Both the 32 MiB and
64 MiB forms compile with production kernel resources unchanged, and all
table, recoding, shared-tree, and SHA-tail audits pass.

A separate generic RTX 4090 microbenchmark then tested the hardware question
without using candidate source.  Its one kernel issued four independent random
128-bit loads per loop step over either a 32 MiB or 64 MiB array.  Baseline and
persisting streams used identical indices and checksums in three alternating,
reverse-order pairs of two-second arms.  The runtime accepted the full 49.5 MiB
set-aside, a 1.0 hit ratio for 32 MiB, and a 0.7734375 ratio for 64 MiB.

| random-load array | baseline median | persisting median | paired ratio |
| --- | ---: | ---: | ---: |
| 32 MiB | 1,184.79 GiB/s | 1,184.53 GiB/s | 0.999781 |
| 64 MiB | 610.69 GiB/s | 610.51 GiB/s | 0.999712 |

The cache hint was neutral within roughly 0.03%, so a longer probe was not
warranted.  The near-halving of logical load throughput after crossing from
32 MiB to 64 MiB is much more consequential and supports cache-capacity
pressure as the reason the 15-term table lost despite doing one fewer mixed
addition.  This microbenchmark is directional evidence rather than a model of
the complete Pinning kernel; the isolated ranked L2-policy submission remains
the authoritative test of interaction with arithmetic and SHA work.

The ranked probe is a coherent host/cache bundle: the policy requires a
contiguous X/Y allocation and it also moves the hot loop onto one explicit
stream with ordered asynchronous copies.  To preserve causal follow-up, a
no-policy twin has been compiled that retains exactly those allocation, stream,
copy, and timer changes while omitting the set-aside request and access-policy
window.  All four device-code sections are byte-identical to the corresponding
promoted control.  If the ranked bundle moves materially despite the generic
policy result, this twin separates host-loop effects from the cache hint.

The ranked host/cache bundle ultimately scored **554,754,810**, a loss of
2,949,689 or **0.5289%** against the 557,704,499 frontier, and was rejected.
Together with the generic ratios near 1.0, this is enough to abandon the L2
persistence direction.  The bundle result does not isolate whether contiguous
allocation, explicit-stream host ordering, or the cache hint caused the loss;
the distinction no longer matters for selection because the combined behavior
is worse and none of those changes is required by another retained candidate.

## Lazy inverse-tree canonicalization

The packed inverse tree performs 765 field products per 256-thread block: 255
to build the tree, 254 to propagate internal inverses, and 256 to return the
leaves.  Its carry-preserving field multiplier already returns an exact residue
in `[0, 2^256)`; the final comparison and subtraction merely select the
canonical representative.  Internal tree nodes can therefore retain a
noncanonical but congruent value.  Normalizing only the root before `_ModInv`
and each returned leaf reduces canonicalizations from 765 to 257 per block,
eliminating 1.984375 small predicate/subtraction sequences per candidate.

The actual 241-instruction PTX multiply was interpreted over 4,928 boundary
and random pairs, including `(p-1)^2`, whose raw result deliberately appears as
`p+1`.  Twenty-four full tree blocks returned all 6,144 expected canonical
inverses.  An independent adversarial review added 12,000 pairs with both
operands in `[p, 2^256)`, 8,200 normalization-boundary representatives, and
five complete tree patterns that propagated 2,072 noncanonical internal
values; lazy and eager leaves matched bit for bit.

On the promoted 16-term geometry the isolated patch keeps 128 registers, a
288-byte stack, zero caller spills, and 24,576 bytes of shared memory while
removing 24 encoded fast-kernel slots and 22 non-NOP instructions.  Combined
with the XYZZ schedule, it keeps the latter's 272-byte stack and spill removal
and removes another 16 slots.  The dynamic saving is real but likely below the
1% promotion threshold by itself, so it is queued as a bundle component after
short GPU evidence or the isolated XYZZ ranked result.

The isolated XYZZ schedule supplied that ranked calibration.  It scored
**557,730,322**, only 25,823 or **0.0046%** above the promoted source, and was
rejected.  Its 32-slot reduction, 16-byte stack reduction, and removal of the
contextual inversion spills therefore had no measurable standalone throughput
effect.  It remains algebraically safe and can relieve pressure in a bundle,
but static stack and code-size improvements no longer count as positive
performance evidence by themselves.  New bundles must remove work executed on
every candidate, such as tree normalizations or address calculations.

## One-pointer interleaved table layout

The accepted table uses separate 16 MiB X and Y planes.  An audited alternative
stores each point as one 64-byte `[X,Y]` record and passes one table pointer
through the builder, debug path, and search kernels.  The footprint remains
exactly 32 MiB, and every lookup still issues four 128-bit loads totaling 64
bytes.  Exhaustive address checks cover all 524,288 points and all 65,536
signed odd digit mappings; 256 independently constructed OpenSSL points also
round-trip through the exact record bytes.

The layout does not reduce requested sectors, so no bandwidth saving is
claimed.  Its concrete native benefit is simpler addressing: the fast kernel
falls from 10,520 to 10,512 slots and from 10,506 to 10,500 non-NOP
instructions, while registers, stack, spills, shared memory, and load count
stay unchanged.  Source/SASS correspondence accounts for about 58 fewer
dynamic integer address operations per candidate: two in the seed and four in
each of fourteen rolled mixed-add iterations.  Pairing X and Y in one page may
also help translation locality, but the device TLB geometry is undocumented.
A generic short layout A/B is prepared to distinguish a real memory-system
effect from the small static address simplification.

## Executed-work cleanup bundle

The next ranked candidate composes four changes on the promoted 16-term,
32 MiB geometry: the reviewed XYZZ live-range schedule, lazy inverse-tree
canonicalization, direct selection between the two recovered x coordinates,
and the interleaved 64-byte point records.  The isolated XYZZ result was neutral,
so it is included only as safe liveness relief.  Lazy canonicalization is the
main executed-work change: it normalizes the tree root before inversion and the
256 returned leaves, removing 508 redundant canonicalizations per block, or
1.984375 per candidate.  Direct selection eliminates the compiler's local
two-pointer array.  The interleaved table preserves the exact 32 MiB footprint
and four 128-bit loads per lookup while simplifying address generation.

The exact final source fingerprints are:

```text
pinning.cu  a3c4f30ff6466aa7d02499cda243169bf63a8f480ea70e0ae606dbfa8fe9c01d
GPUMath.h   02f06487bfd277ccfedad45dda5684125f0ff4f61cd7a89bd1e586ea8b2461d8
GPUHash.h   8cf9b303b6f5a09051e433a8bc21e2f3a66631e3bd22b2b1a10e71fe0b21b2bc
```

An exact CUDA 12.6.85 `sm_89` build uses 128 registers, 24,576 bytes of
shared memory, a 224-byte fast-kernel stack, and zero caller spill bytes.  Its
fast text is 167,424 bytes (10,464 encoded slots and 10,454 non-NOP
instructions), versus 168,320 bytes, 10,520 slots, 10,506 non-NOPs, and a
288-byte stack for the promoted control.  The contextual `_ModInv` 16-byte
spill loads and stores disappear.  The linked executable hash is
`a4d6bd5e88be343d6a88d27eba80bf90dbb408df611736a33acf23c5c19eab58`;
the extracted fast native-text hash is
`6149f99c8d142b76f38d4cdd1b62449d41e5bda730f472865a7601786b4c0d72`.

Fresh checks on the exact source passed 102,000 raw XYZZ DAG comparisons, 300
curve additions, 600 equal/opposite cases, 10,000 arbitrary source comparisons,
300 libcrypto comparisons, 4,928 interpreted PTX products, 24 complete lazy
trees with 6,144 leaves, five adversarial trees with 2,072 deliberately
noncanonical internal states, 8,200 normalization-boundary values, 211 packed
shared-tree layouts, 200,000 pointer selections, exhaustive addressing for all
524,288 table points and all 65,536 signed digits, 256 independent OpenSSL
point records, and 2,320 SHA-tail cases.  No modified-source GPU score is
claimed; the ranked validator is the first complete timing measurement.

After the ranked archive was queued, a self-contained public-data RTX 4090
microbenchmark isolated the final table-layout question.  Both kernels used a
32 MiB table, 256 blocks of 256 threads, sixteen chunk-constrained random point
lookups per synthetic candidate, identical point bytes, four 128-bit loads per
lookup, 40 registers, and no stack or spill traffic.  Four alternating-order
pairs used half-second arms.  Every repeated launch produced the same nonzero
checksum in both layouts.

| layout | median synthetic candidates/s | paired ratio |
| --- | ---: | ---: |
| split 16 MiB X + 16 MiB Y | 2,121.18 M | 1.000000 |
| interleaved 64-byte X/Y record | 2,256.90 M | **1.064064** |

The individual paired ratios were 1.06254, 1.06412, 1.06405, and 1.06408.
This establishes a repeatable 6.4% advantage for address generation and the
memory access pattern in isolation.  It does not imply a 6.4% end-to-end gain,
because the production kernel also performs fixed-base curve arithmetic,
batched inversion, and SHA-256.  It does strengthen the causal case for the
interleaved component and will help interpret the combined ranked score.

## Fifteen-term radix-373 GLV design

The next algorithmic target uses the secp256k1 endomorphism rather than a wider
one-dimensional table.  The GLV decomposition writes
`k = k1 + lambda*k2 (mod n)`, where applying `lambda` to an affine point keeps
`y` and multiplies `x` by the field cube root `beta`.  Bitcoin Core's
`libsecp256k1/src/scalar_impl.h` documents the exact lattice basis, rounded
`g1`/`g2` division constants, and 128-bit component proof used here; the
underlying method is Gallant, Lambert, and Vanstone, *Faster Point
Multiplication on Elliptic Curves with Efficient Endomorphisms* (CRYPTO 2001).

A rational Eisenstein radix of 373 has `373^2 = 139129` two-dimensional
residue classes.  The 139,128 nonzero classes form exactly 23,188 free orbits
under the six units `+/-{1,lambda,lambda^2}`.  Fifteen chunks with one 96-byte
record per orbit occupy:

```text
15 * 23188 * 96 = 33,390,720 bytes = 31.843872070 MiB
```

Each record can hold one common `y` and two x orientations.  The third follows
from `beta^2*x = -x-beta*x (mod p)`, so approximately one third of digits need
one four-limb field addition/negation instead of another 32-byte load.  Sign is
free through `y -> p-y`.  This preserves the successful roughly 32 MiB table
footprint while reducing fixed-base accumulation from sixteen points to
fifteen: one 64-byte point lookup and one `8M+2S` mixed XYZZ addition disappear
per candidate.  A small 32-bit map from residue pair to orbit/orientation/sign
is approximately 544 KiB and should remain hot in L2.

The difficult coverage condition now has an exact proof and executable audit.
Choose the unit-invariant nearest representative for each residue class.  Its
Eisenstein norm is at most 46,129 and each of the three Voronoi support forms
`2a-b`, `2b-a`, and `a+b` has absolute value at most 373.  The device starts
from the published rounded split and selects the first of the raw point and
its six shortest lattice neighbours whose support forms fit the exact bound
`T = 373^15 - S - 1`.  Trying the four basis neighbours first covers the hot
cases; the final `+/-(v2-v1)` pair completes the nearest-cell argument on a
cold branch.

After fourteen radix divisions, a support form of the remaining quotient is
bounded by

```text
(T + 373*(373^14-1)/372) / 373^14 < 373.
```

It is integral, hence at most 372.  This lies strictly inside the final
radix-373 Voronoi cell, so the fifteenth digit equals the remaining quotient
and the next quotient is exactly zero.  Thus every scalar has exactly fifteen
digits; this is a coverage proof rather than a random-search inference.

`audit_glv_radix373.py` exhaustively constructs all 139,129 residue mappings,
checks all 23,188 unit orbits and the proof inequalities, and verifies scalar
congruence, exact reconstruction, table orientation, and termination.  The
current deterministic run added 100,000 random scalars plus boundary cases;
all terminated, and the observed fourteenth-quotient support maximum was 372
against the proven 372 limit.  An earlier 200,000-scalar nearest-cell run also
had zero failures.  Zero digits occur with
roughly `1/139129` probability per chunk and need a rare divergent slow path;
the common path remains a fixed fifteen-point accumulation.

The rounded split and complete radix recoder have now been implemented and
checked against independently generated exact vectors.  A self-contained
RTX 4090 microbenchmark measured about 13.67 billion raw splits/s and about
4.0 billion complete split/adjust/recode operations/s.  A separate public-data
table geometry probe measured the 96-byte GLV layout at 1.0810 times the
synthetic lookup rate of the current 64-byte, 32 MiB layout; storing all three
x orientations in 128-byte records was slightly slower than the control.

## Ranked result: executed-work cleanup bundle

The combined lazy-normalization, direct-selection, XYZZ-schedule, and
interleaved-table candidate scored **566,048,339 verified candidates/s** and
was promoted.  That is 8,343,840, or **1.4960%**, above the previous
557,704,499 frontier.  Its promoted source commit is `5724411`; the next exact
1% promotion cutoff is 571,708,823.

The result validates the bundle but does not identify a single causal change.
The isolated XYZZ schedule was already neutral at +0.0046%, and lazy
normalization removes only 1.984375 short canonicalization sequences per
candidate.  The corrected public-data layout probe measured a stable 6.4064%
memory-only advantage, so interleaving is the strongest causal explanation,
with lazy normalization and direct selection contributing or composing around
it.  No component is removed from the production base until an ablation has
enough expected value to justify a ranked run.

The 1.496% promotion also calibrates the next search: static cleanup bundles
can clear the threshold when they change table addressing, but another
promotion now needs at least 5,660,484 candidates/s.  The GLV design targets a
whole mixed addition and lookup rather than another compiler-only delta.

## Implemented GLV experiment

The radix-373 design is now integrated as a ranked experiment. `GLVTable.h`
contains the exact rounded 256-to-two-128-bit split, the support-bounded lattice
adjustment, mixed-width radix division, orbit decoding, and fifteen-term XYZZ
accumulation. `GLVBuilder.h` constructs the residue map, builds the 31.843872
MiB runtime table from short `A` and `lambda*A` ladders, and checks table records
against OpenSSL. The table record is `[x, beta*x, y]`; rotation two reconstructs
`beta^2*x = -x-beta*x`, while the sign bit conditionally replaces `y` by `p-y`.

The common path starts with the first two affine points and performs thirteen
remaining mixed additions. A zero among those first two digits enters a
no-inline exact recovery routine. Later zero digits are skipped directly, so
they no longer cause a redundant complete multiplication. A 32-scalar startup
GPU/OpenSSL parity test covers ordinary values, group-order boundaries, and
known scalars with zero digits in chunks 0, 4, and 14. This checks the complete
device split, recoder, orientation logic, additions, and affine result before
the timed search begins.

An exact CUDA 12.6 `sm_89` build uses 128 registers, 24,576 bytes of shared
memory, a 400-byte fast-kernel stack, and zero fast-kernel spill loads/stores.
The cold recovery call increases the declared frame relative to the promoted
224-byte control, but the save/restore path is conditional. The standalone
device-like implementation matched 10,009 independently generated exact split
and full-recode vectors. The Python proof audit exhaustively checked all 139,129
residues and 23,188 orbits, then passed 100,012 boundary/random scalar
reconstructions with no tail failures. The ranked validator remains the first
complete performance and device parity measurement for the integrated source.

## Radix-384 pipelined follow-up

While the radix-373 archive waited for validation, a parameter sweep exposed a
useful discontinuity. The exact 15-digit support threshold first contains the
nearest GLV cell at radix 373, but the entire independent-rounding
parallelogram first fits at radix 383. Radix 384 is one step larger and factors
as `3*128`, making quotient/remainder extraction cheaper. For a raw split
`z = f1*v1 + f2*v2` with `|f1|,|f2| <= 1/2`, the maximum of each linear support
form occurs at a parallelogram corner. The three exact maxima are all below
`384^15 - 384*(384^14-1)/383 - 1`, with the tightest using 95.06% of the bound.
Thus no per-scalar support check or lattice adjustment remains.

Because 384 is even and divisible by three, two nonzero unit orbits have sizes
two and three instead of six. Exhaustive enumeration gives 24,575 ordinary
six-element orbits plus one of each exceptional size, or 24,577 stored records
per chunk. The 96-byte table becomes 35,390,880 bytes = 33.751373291 MiB.
`audit_glv_radix384.py` checks every one of the 147,456 residue pairs, both
exceptional orbits, the raw-split bound, exact scalar reconstruction, and
termination. Its 100,012-scalar run had no tail failures.

A four-round public RTX 4090 lookup A/B found the larger table essentially tied
with radix 373: 2,516.60 versus 2,518.02 million synthetic candidates/s, both
about 1.081 times the current one-dimensional layout. The complete public
split/recode probe improved from roughly 4.0 billion/s at radix 373 to
4.189 billion/s at radix 384. Replacing division by 384 with an explicit
shift-by-seven followed by exact reciprocal division by three raised that to
4.211 billion/s. The follow-up therefore keeps the same fifteen curve terms
and submits the simpler raw-split path as an independently scored delta.

## Hybrid block/warp batch inversion experiment

The promoted v19 normalization path batch-inverts 256 projective `z` values
with a full binary tree in shared memory. Eight upward levels and seven
downward levels require repeated whole-block synchronization. The current
experiment retains the first three block-wide reductions, leaving 32 products
that each cover eight lanes. Warp zero then uses the existing shuffle-based
batch-inversion primitive to invert those 32 subtree products with one scalar
`_ModInv`. Two block-wide downward levels expand the subtree inverses before
the unchanged leaf reconstruction.

This changes only `qsb_block_inverse` relative to the exact promoted v19
runtime source. It removes ten block barriers and the top shared-memory tree
passes. The packed limb-major arrays shrink from `products[4][512]` and
`inverses[4][256]` to `products[4][480]` and `inverses[4][224]`, reducing
static shared memory from 24,576 to 22,528 bytes. The final 64-to-32 product
level is consumed by the same lanes that write it, so its CTA fence is omitted;
the complete path executes six CTA barriers instead of sixteen. An exact CUDA 12.6 `sm_89`
build reports 128 registers, a 224-byte fast-kernel stack, and no fast-kernel
spill loads or stores. Fast-kernel text grows from 167,424 to 176,512 bytes
because the shuffle exchange is instruction-heavy, so the ranked outcome will
measure the barrier-latency saving against that additional instruction issue.

`audit_hybrid_inverse.py` models the source offsets and active-lane identity
mapping directly. It checks zero, one, full-width, irregular tail, and random
active masks against independent modular inverses; 211 deterministic cases
pass. The experiment is intentionally isolated from the queued GLV changes so
its ranked result can identify the inverse-tree effect.

## Final-carry field-reduction audit

An independently inspected submission identified a correctness edge in the
sparse double-fold field reduction. After the second fold, the current v19
multiply and square store the low 256 bits and drop a possible carry. For the
valid field input `a = p - 65537`, the square path produces a carry of one:
the dropped result is `0x1fc30`, while the exact residue is `0x100020001`.
Folding the carry by `2^256 == 2^32 + 977 (mod p)` recovers the exact value.

The correction is algebraically valid for every 256-bit input. After the first
fold the high part is bounded by `2^32+977`; after the second fold the whole
value is below `2^256 + 2^66`, so its final carry is at most one and the low
remainder is small whenever that carry occurs. Adding `2^32+977` cannot
overflow again in that case. This path is rare for uniformly distributed field
values, while the extra instructions sit in every hot multiply and square. It
is therefore retained as a separate correctness/performance experiment rather
than being bundled into the hybrid inversion submission.

## External root inversion with internal-node checkpoints

The next experiment returns to the exact promoted `5724411` runtime and moves
the single product-tree root inversion out of the 256-thread search kernel.
Submission `4745a544` by `odinfree` supplied the material unpromoted insight:
isolating roots in a dense kernel avoids keeping 255 idle threads resident
while lane zero executes `_ModInv`. The implementation here independently
adapts that idea to the promoted interleaved-table and lazy-normalization base,
then removes the duplicated work that limited the sibling experiment.

The prepare kernel computes the same 256 leaves and upward product tree as
v19. It publishes one root per CTA and checkpoints internal nodes 256 through
509. Four fields cross the kernel boundary: `C=ZZ*d^2`, `Y`, `W=ZZ^2*d`, and
`ZZZ`. This is the same 128-byte state footprint as retaining `d,Y,ZZ,ZZZ`, but
it preserves each leaf `W` and moves the `C` square and multiply from finish to
prepare. The middle kernel launches 128 threads per block over the compact root
array; every active thread normalizes one root and runs `_ModInv`. The finish
kernel restores its saved `W` leaves and 254 internal nodes, expands the root
inverse downward, normalizes the 256 returned leaves, and evaluates the same
two direct-XYZZ recovery formulas.

For `N` candidates and `B=ceil(N/256)` CTAs, the cross-kernel logical traffic
is `256N + 16384B` bytes: 128-byte state write and read per candidate, 8,128-byte
internal-node write and read per CTA, and 128 bytes of total root traffic per
CTA. A full CTA therefore transfers 320 bytes/candidate; a 16M batch transfers
exactly 5 GiB. The padded allocations are 2,048 MiB for state, 512 MiB for tree
nodes, and 2 MiB for roots. At the v19 frontier rate, 320 bytes/candidate is
about 181 GB/s of logical traffic, leaving substantial headroom against RTX
4090 DRAM bandwidth while keeping every large access lane-coalesced.

The arithmetic is unchanged. Recovery performs `2L + 8U + 765B` field
multiplies, `L + 3U` squares, and `B` inversions, where `L=256B` lanes and `U`
is the number of usable candidates. A full usable CTA including fixed-base
work executes `33,021M + 8,704S + 1I`. The split executes eight CTA barriers in
prepare and eight in finish, equal to v19's sixteen; the default stream's two
kernel boundaries provide the cross-kernel ordering. Normalization remains
exactly one root immediately before inversion and one result per returned
leaf.

CUDA 12.6.85 for `sm_89` reports 128 registers, 64 bytes of stack, 16 KiB of
shared memory, and zero spills for the fast prepare kernel; 94 registers,
64 bytes of stack, 24 KiB shared, and zero spills for finish; and 96 registers,
120 bytes of stack, no shared memory, and zero spills for the root kernel. The
promoted combined fast kernel used 128 registers, a 224-byte stack, and 24 KiB
shared. Static fast-path text grows from 10,464 slots to 12,152 across all three
kernels, but each independently scheduled candidate kernel is smaller than the
monolith.

`audit_external_pipeline.py` independently reconstructs the checkpointed tree
and compares the `C/W` cut against the original recovery algebra. It passed 210
zero, tail, full, and random active-lane trees plus 10,000 recovery cases. It
also source-checks the exact node bounds, normalization order, state transform,
launch stages, and allocation stride. The production signed-window audit was
repaired to match the promoted 16-by-16-bit geometry and now passes 3,564
boundary scalars, 50,000 random scalars, every one of 524,288 table offsets,
and every builder decomposition. The original shared-tree and SHA-tail audits
pass 211 and 2,320 cases respectively.

## Arithmetic screens kept out of production

A correctness-first 128-by-128 Karatsuba field multiplier reduced the wide
32-bit product count from 73 to 57, but its exact `sm_89` probe expanded from
168 to 240 SASS slots. Recombination raised `IADD3` from 42 to 93, used 21
additional `LOP3` instructions, and increased registers from 40 to 44. An
independent 150,565-case audit covered all sign quadrants, noncanonical inputs,
aliasing, and final-carry cases. The measured instruction expansion rejects
Karatsuba for this GPU despite the lower multiplication count.

A fused `a-b-c-c` pseudo-Mersenne subtraction for the XYZZ `X3` formula passed
201,000 exact congruence cases. A standalone mixed-add probe fell by only eight
SASS slots (1,456 to 1,448), with identical 674 `IMAD.WIDE` instructions and
two fewer registers. The projected gain is below the one-percent promotion
threshold, so it remains an additive follow-up rather than contaminating the
first external-pipeline measurement.

## Ranked GLV calibration

The radix-373 GLV experiment scored **499,336,895 verified candidates/s**
against the 566,048,339 promoted frontier, a direct regression of 66,711,444
or **11.79%**. One fewer table lookup and XYZZ mixed addition did not repay the
two-dimensional recoding/orbit decode, larger records, cold zero-digit path,
and 400-byte kernel frame. This decisively rejects the current GLV integration
despite its favorable standalone recoder and lookup probes.

The queued radix-384 follow-up changed only the split/recode mechanism and
slightly enlarged the same table shape. It could not plausibly recover the
13.36% improvement required to move from the measured radix-373 result to the
current promotion cutoff, so validation was canceled before completion. This
preserves ranked capacity for architectural experiments with independent
upside rather than spending another full run on a dominated sibling.

## Hierarchical root batching and vector checkpoint I/O

The external-pipeline follow-up removes the remaining scalar-inversion fanout.
A full 16,777,216-candidate launch produces 65,536 search-CTA roots. The prior
pipeline normalized and inverted every root independently. This version groups
those roots into 256 checkpointed product trees, builds one final 256-leaf tree
over the group roots, executes `_ModInv` once, and expands the inverse through
both tree levels. Thus the full batch uses one scalar inversion rather than
65,536. Partial search batches use the same fixed-width collectives: inactive
candidate lanes, unused roots in the last group, and unused group roots in the
top tree all contribute the field identity.

The new root path has three compact kernels. `qsb_root_group_prepare` reads up
to 256 main roots per CTA, computes 255 tree products, checkpoints nodes 256
through 509, and publishes the group root. `qsb_invert_super_roots` launches
exactly one 256-thread CTA and reuses the original shared-memory block inverse
over the at most 256 group roots. Its lane-zero root is normalized immediately
before the sole `_ModInv`. `qsb_root_group_finish` restores each group's
immutable nodes, accepts the canonical group-root inverse, and expands it to
canonical inverses for the original search CTAs. Default-stream launch order is
prepare, group prepare, super inverse, group finish, and candidate finish.

For a full batch, the extra level performs 196,605 field multiplications: 765
per root group plus the 765 multiplications in the final block inverse. This is
about 0.01172 multiplication per searched candidate. The hierarchy retains
the old read/write of every main root and adds about 6 MiB of checkpoint and
super-root traffic, exactly 0.375 byte per candidate. Its added allocations are
2 MiB for padded group-tree checkpoints and 8 KiB for the group roots. Together
with the 2,048 MiB candidate state, 512 MiB first-level checkpoint, and 2 MiB
main-root buffers, the pipeline allocates about 2,564 MiB.

The checkpoint state also changes from sixteen scalar 64-bit SoA planes to
eight `ulonglong2` planes. Adjacent limbs of each of `C`, `Y`, `W`, and `ZZZ`
share one naturally aligned vector. The logical layout remains bijective and
the allocation remains exactly 128 bytes per candidate, or 2 GiB for the full
batch. CUDA allocation alignment and the 16-byte plane stride make every
element aligned. Actual `sm_89` SASS now contains eight `STG.E.128` state
stores in prepare and eight `LDG.E.128` state loads in finish, replacing
sixteen scalar operations in each direction. Tree and root traffic remains
scalar and separate.

The combined CUDA 12.6.85 build has zero spills in every ranked kernel. Fast
prepare uses 127 registers, 64 bytes of stack, 16 KiB shared, and 6,208 SASS
slots. Fast finish uses 94 registers, 64 bytes of stack, 24 KiB shared, and
4,488 slots. Slow prepare uses 128 registers, 256 bytes of stack, 16 KiB
shared, and 6,536 slots; slow finish has the same 94-register/4,488-slot shape
as fast finish. The group prepare and finish kernels use 44 registers, zero
stack, 16/24 KiB shared, and 232/400 slots. The single super-root inverse uses
114 registers, 120 bytes of stack, 24 KiB shared, and 1,768 slots. Relative to
the scalar-I/O external pipeline, vectorization removes 144 slots from the fast
prepare/finish pair and lowers fast-prepare allocation by one register.

`audit_superbatch_roots.py` independently models both tree levels and checks
137,492 roots over twelve boundary sizes from one through 65,536. Every returned
value multiplied by its input equals one modulo the field prime. The source
checks require the exact five-kernel order, group-count ceilings, and reuse of
the checkpoint helpers. `audit_vector_state_layout.py` proves the old and new
byte layouts contain exactly the same logical cells, checks all 16-byte and
warp-contiguity invariants, and verifies the production allocation is exactly
2 GiB. The external-pipeline audit still passes 210 split trees and 10,000
recovery comparisons; the original product-tree, final-carry, signed-window,
and SHA-tail audits also pass. Performance remains intentionally unclaimed
until ranked hardware measures the extra tree arithmetic and launches against
the eliminated exponentiations and narrower checkpoint instructions.

## Compile-time removal of the unreachable second-hash path

The ranked host selects `FAST_TAIL=true` only when `single_hash && !easy` and
the input has the exact 75-byte/31/67/9995 pinning layout. Inside the templated
finish kernel, however, the original `if (single_hash) continue` depends on a
runtime argument. PTXAS therefore retained the complete second public-key hash
in the fast specialization even though the host can never request it.

Changing that guard to `if (FAST_TAIL || single_hash) continue` exposes the
existing host contract at compile time. It does not alter any reachable case:
`FAST_TAIL=true` already implies `single_hash=true`, while for
`FAST_TAIL=false` the expression reduces to the original runtime condition.
The easy and double-hash modes continue to use the fallback specialization.

CUDA 12.6.85 `sm_89` reduces fast finish from 4,488 to 3,136 SASS slots, a
1,352-slot or 30.12% reduction. Registers fall from 94 to 92; the 64-byte
stack, 24 KiB shared-memory allocation, and zero-spill result are unchanged.
Fast prepare and every fallback or auxiliary kernel are instruction-identical
to the hierarchical control. `audit_fast_tail_contract.py` binds the source
predicate, template launches, guard position, and second-hash code, then checks
that the second hash executes exactly when `single_hash` is false across the
host selection matrix.

A separate inline H0-only compressor was screened after this correction. Once
the unreachable second hash was removed, returning only digest word H0 saved
just eight more slots and did not reduce the 92-register allocation. A public
no-inline H0 submission had also regressed relative to its full-SHA sibling.
The H0 change is therefore omitted so the ranked delta remains the larger,
safer compile-time path elimination.

## Finish occupancy and local-state cleanup

The next bounded compiler screen removed the address-taken recovered-key
selector, scoped checkpoint reloads to active lanes, propagated the existing
fast normal-mode contract, and applied a finish-only three-CTA launch bound.
Selecting the four x-coordinate limbs directly eliminates four 128-bit local
stores and two rolled 128-bit local loads per candidate.  The active-lane
scope removes sixteen zero initializations which are dead after the collective,
and the compile-time easy predicate removes eight more fast-path slots.

Together with `__launch_bounds__(256, 3)` on stage two, CUDA 12.6.85 emits a
3,096-slot fast finish and 4,464-slot fallback finish.  Both use 80 registers,
zero stack, zero spills, and 24 KiB shared memory.  Three CTAs therefore need
61,440 of AD102's 65,536 registers and 72 KiB shared memory.  The promoted
control used 4,488 slots, 94 registers, and a 64-byte stack.  Unrolling the
two-pubkey loop was rejected because it duplicated the compression image and
grew the fast finish to 4,440 slots.  Applying the same occupancy bound to
prepare was also rejected because it caused substantial spills.

The exact finish bundle was submitted as
`5583a492-1416-4440-befc-937941b285f4`; its validation was still queued when
the following prepare-stage composition was archived.  Its result is a gate:
if lower-register scheduling loses more per-warp latency than the third CTA
hides, the composition should be canceled before it reaches the runner.

## Streamed signed recoding and safe diagnostic strip

The materialized `int32_t gte[16]` digit array occupied a 64-byte local stack.
Four `STL.128` instructions wrote it, and the rolled fixed-base loop loaded one
digit on each of sixteen iterations.  The replacement keeps the four-limb
Joye-Tunstall state in registers and calls the unchanged recurrence immediately
before each table lookup.  It preserves all sixteen signed odd digits, table
indices, and point additions while removing 64 local bytes in each direction
per candidate.

Fast prepare changes from 6,208 slots / 127 registers / 64-byte stack to 6,056
slots / 128 registers / zero stack.  Fallback prepare changes from 6,536 slots
/ 128 registers / 256-byte stack to 6,400 / 128 / 192 bytes.  Both remain at
two CTAs and have no compiler-reported spills.  A forced three-CTA variant,
no-inline fixed-base boundary, and no-inline SHA boundary were rejected for
spill or local-argument traffic.  The performance uncertainty is whether the
new recurrence dependency inside the add loop offsets the removed local I/O.

Public unpromoted submission `084b0f1a-baf9-4654-b204-59122bfe35cc` by
`@odinfree` identified a large diagnostic call graph that could be removed.
Its exact source and patch were independently built and audited.  The safe
hierarchical port deletes the debug kernel and unreachable affine/on-curve
helpers, but retains `qsb_block_inverse`, which is live in the super-root
kernel here.  All eight live SASS sections remain byte-identical.  The exact
composed cubin shrinks from 795,296 to 430,880 bytes and compile time falls by
roughly half; this can affect only build, module-load, or JIT overhead, since
live device instructions do not change.

The final production source SHA-256 is
`7c1f43ebb8a7cb564d72d2acd19fe81dfb2f6f65546877fd3b7efc35411ac6ca`;
its exact `sm_89` cubin SHA-256 is
`5bba816bcdaed024afbed7105caec8288722d7e7ad1b1724677fb51b8595f1b9`.
The recode audit passed 201,404 boundary and random scalars.  Inherited tests
passed 210 split trees and 10,000 recovery cases, 137,492 two-level roots,
all vector-layout and 2 GiB allocation checks, 211 shared trees, 200,576
final-carry cases, 53,564 signed-window scalar cases plus all 524,288 table
offsets, and 2,320 SHA-tail comparisons.  A fresh CUDA build reproduced the
source and cubin hashes before archive creation.

## Mixed table addressing and a fully deferred affine anchor

The mixed 15-chunk schedule doubles the table to 64 MiB but removes one XYZZ
addition. Its first chunk has an 18-bit signed odd digit and the other fourteen
have 17-bit digits. The rolled production loop now carries the flat base of
chunk 2 and advances it by 65,536 entries per iteration. This is algebraically
the same as `gt_offset(c)`, but CUDA 12.6 emits the recurrence as uniform
address arithmetic. On `sm_89` it removes the seed candidate's one-time local
spill, lowers both prepare kernels from 128 to 127 registers, and removes 24
SASS slots. `audit_stream_recode.py` exhausts the 1,048,576-entry map as well
as 51,404 scalar recodings and binds the flat recurrence to the source.

The ordinate is then kept in affine-anchor-deferred form through the complete
rolled addition chain. If an XYZZ point represents actual ordinate `Y` and
stores `Yd = Y + a*ZZZ`, adding affine `(xq,yq)` obtains the ordinary slope
numerator from `(yq+a)*ZZZ - Yd`. The addition can omit the final
`yq*ZZZnew` multiplication and store its core ordinate instead, making `yq`
the next anchor. This induction applies to every intermediate table point.
Only the final addition resolves the ordinary ordinate required by recovery.

The two-point deferred prefix costs 3M+2S, twelve intermediate additions cost
7M+2S each, and the resolving final addition costs 8M+2S. The complete
15-point fixed-base sum therefore falls from 108M+28S before the seed work and
107M+28S in the first deferred-seed archive to **95M+28S**. In native code the
uniform `c != 14` path skips 143 instructions, including all 73 wide multiply
instructions of one field multiplication, exactly twelve times per candidate.
The fast prepare kernel uses 124 registers, no stack or spills, and 5,976 SASS
slots; fallback prepare uses 124 registers, a 192-byte suffix stack, no spills,
and 6,320 slots. Finish, builder, and hierarchy kernels are unchanged.

`audit_deferred_chain.py` independently compared the old exact chain and the
deferred recurrence on 20,000 arbitrary-field accumulations, 1,000 curve
accumulations, and 1,000 complete mixed-window accumulations. It also checks
the production source form and the invariant after every intermediate point.
All inherited field, root, vector-state, finish, and SHA-tail audits pass.
